print('"a"'),则输出带双引号的a
<表达式1>if<条件>else<表达式2>
try:
except:
else:
finally:
以上为异常处理，try错误时执行except，正确是执行else，最后无论对错，都要执行finally
for i in range(x,y,k) 从x到y，以k为步长
continue结束当次循环  break结束当前循环(最内层循环)
S|T为并集   S&T为交集  S-T为在S中不在T中  S^T为S和T中的不相同元素
S<=T S<T 判断是否为子集 S>=T S>T 判断是否为包含
对序列可进行索引，切片，用+号连接，用*号复制
序列，字典，集合可遍历
r只读，不存在则返回错误
w覆盖写，存在则覆盖，不存在则创建
x创建写，存在则返回错误，不存在则创建
a追加写，存在则在最后增加内容，不存在则创建
b二进制模式，t文本文件模式，与rwxa一起使用
[:]创建列表副本
*a,定义任意数量的实参
**a，定义一个字典参数
r' '内不用转义
print('%s-%s' % (x,y))   
这也可以用来格式化，d为整数，f为浮点数，s为字符串，x为十六进制，b为二进制，o为八进制
f=函数名,可以讲函数赋给变量